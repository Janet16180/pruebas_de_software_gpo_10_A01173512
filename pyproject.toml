[project]
name = "pruebas-de-software-gpo-10-a01173512"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "coverage>=7.13.4",
    "flake8>=7.3.0",
    "prek>=0.3.1",
    "pylint>=4.0.5",
    "ruff>=0.15.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]

[tool.ruff]
line-length = 79

[tool.ruff.lint]
extend-select = [
    "ANN",  # Type annotations
]

[tool.ruff.lint.per-file-ignores]
"**/tests/**" = ["ANN"]
